# Единый план переезда JTD на VPS (актуальный)

> Обновлено: 2026-07-05. Этот файл — единая точка правды по переезду.
> Заменяет устаревшие предположения из PHASE-*.md (там был расчёт на pg_dump,
> которого у нас нет — Lovable Cloud не даёт доступ к БД напрямую).

---

## Что изменилось по сравнению с исходным планом

| Было в плане | Стало в реальности |
|---|---|
| РФ VPS (Selectel/Timeweb) | **Иностранный VPS Sweb** `189.74.120.232` |
| pg_dump всех схем → pg_restore | **CSV-экспорт** от Lovable (нет доступа к pg_dump) |
| auth.users переносятся с паролями | **Сброс паролей** после переезда (auth недоступен) |
| Доступ из РФ — главный риск | ✅ Проверено: VPS отвечает из РФ без VPN |

---

## Архитектура после переезда

```
Пользователь (РФ, без VPN)
        ↓ https://justtodoit.ru
   nginx на VPS (189.74.120.232)
        ├─ / → фронтенд (dist/)
        └─ /sb/ или api → Kong :8000
                            ↓
              Supabase self-hosted стек
              (auth, rest, realtime, storage, edge)
                            ↓
                     PostgreSQL 15
```

Lovable продолжает разработку → пушит в GitHub → GitHub Actions
собирает и деплоит на VPS + применяет миграции (deploy-vps.yml).

---

## Статус компонентов

| Компонент | Статус |
|---|---|
| VPS сеть (новый IP /24) | ✅ работает |
| SSH-доступ | ✅ работает |
| Supabase стек (11 контейнеров) | ✅ Up |
| Схема БД (60 таблиц) | ✅ применена |
| Данные (31 таблица из CSV) | ✅ импортированы |
| Данные задач/проектов | ⬜ пустые (зависят от auth.users) |
| auth.users (пользователи) | ⬜ сброс паролей после cutover |
| Доступ из РФ без VPN | ✅ проверено |
| Фронтенд собран | ⬜ |
| nginx + домен | ⬜ |
| SSL | ⬜ (после DNS) |
| DNS переключён | ⬜ |

---

## Оставшиеся шаги

### Шаг 1 — Уведомить пользователей (заранее)
Сообщение 56 пользователям:
> «Переезжаем на новый сервер для стабильного доступа из России.
> После переезда нужно один раз сбросить пароль по ссылке из письма.
> Все ваши задачи, проекты и переписка сохранятся.»

### Шаг 2 — Досоздать данные, зависящие от пользователей
Таблицы `tasks`, `task_groups`, `task_comments`, `profiles` ссылаются на
`auth.users`. Порядок:
1. Создать пользователей в VPS Supabase с **теми же UUID** (из CSV profiles)
2. Отключить FK-проверку, залить CSV задач/проектов/комментариев
3. Включить FK обратно, проверить целостность

```bash
# UUID берём из profiles.csv, создаём через Admin API GoTrue
# (скрипт создадим на этом шаге)
```

### Шаг 3 — Собрать фронтенд
```bash
cd /opt/jtd
# .env для сборки:
#   VITE_SUPABASE_URL=https://justtodoit.ru
#   VITE_SUPABASE_ANON_KEY=<ANON_KEY>
npm ci && npm run build
```

### Шаг 4 — nginx + домен
- Настроить `nginx-full.conf`: фронтенд + прокси на Kong
- Проверить проксирование WebSocket (realtime)

### Шаг 5 — Переключить DNS (точка невозврата)
```
justtodoit.ru   A   189.74.120.232   TTL=60
```
За 24ч до этого — снизить TTL.

### Шаг 6 — Выпустить SSL
```bash
certbot --nginx -d justtodoit.ru
```

### Шаг 7 — Сброс паролей и smoke-test
1. Разослать всем reset-password через GoTrue
2. Проверить: вход → задачи → чат → realtime → Telegram

---

## Принцип безопасности

- Облачный Lovable Supabase **не трогаем** — он остаётся рабочим до и после
  переезда как источник и standby.
- Откат до Шага 5 — мгновенный (DNS не переключён).
- После Шага 5 откат — возврат DNS на Lovable.

---

## Открытые вопросы / риски

| Риск | Митигация |
|---|---|
| Пользователи не сбросят пароль | напоминания, поддержка входа вручную |
| auth.users через поддержку Lovable (запрошено) | если дадут — перенесём пароли, сброс не нужен |
| Telegram-бот на новом IP | проверить webhook после cutover |
| AI Gateway из иностранного VPS | проверить ключ на смоук-тесте |

---

## ✅ GO-LIVE состоялся (2026-07-06)

Команда работает на VPS без VPN. Пароли розданы (временные), пользователи
меняют в профиле. Данные актуальны (59 юзеров), auth работает, edge-функции
живы, Telegram webhook на VPS.

**Приоритет теперь — защита живых данных и добивание бэклога в спокойном окне.**

---

## ✅ Закрыто после GO-LIVE (2026-07-06)

- ✅ Синк main → VPS на актуальной версии приложения (не 3-недельной)
- ✅ AI на OpenRouter (14 функций, ушли от Lovable AI Gateway), ключ в .env
- ✅ Фича смены пароля (пользователи меняют временные пароли)
- ✅ 414 URI Too Long (буферы Kong)
- ✅ Telegram webhook + cron → на VPS; edge-диспетчер (был заглушкой) → рабочий
- ✅ fix_auth_users.sql — идемпотентная защита входа при перезаливках
- ✅ MAX-мессенджер (токен + webhook на VPS)
- ✅ Бэкапы в S3 (проверенные, MD5 совпал; попутно фикс segfault pg_dump)
- ✅ Realtime — живые обновления (тенант через Admin API + DB_ENC_KEY 16 байт)
- ✅ Дельта задач из облака = 0 (обе post-cutoff задачи уже на VPS)

**🎉 Переезд функционально завершён. Остались только косметика/удобства.**

---

## Бэклог — статус на 2026-07-15

Отложенные задачи, зафиксированы чтобы не потерялись между сессиями.

### ✅ Закрыто с прошлой сверки
1. ~~Финальная перезаливка данных~~ — сделана (экспорт от 5 июля, 67 таблиц).
3. ~~414 URI Too Long~~ — пофикшено (буферы nginx+Kong), см. PROGRESS.md.
4. ~~Realtime 403~~ — пофикшено (tenant secret 16 байт через Admin API).
6. ~~Раздача паролей 54 пользователям~~ — сделана.
7. ~~Авто-деплой~~ — **сделано 2026-07-15**. Секрет `VPS_SSH_KEY` в GitHub
   содержал не тот приватный ключ (`github_vps`, никогда не был в
   authorized_keys) — заменён на `gh_deploy` (реально авторизованный).
   `Deploy to VPS` (deploy-vps.yml) теперь проходит end-to-end: push в
   main → SSH на VPS → `deploy.sh` (build + миграции + nginx). Проверено
   рабочим прогоном.
8. ~~Фича «дружелюбная смена пароля»~~ — сделана, работает.
13. ~~AI через OpenRouter~~ — сделано, 14 функций переведены, ключ в
    `.env.supabase`.
14. **STM Mission Control — 4 бага пользователей** — сделано 2026-07-15
    (архив-таймер, RLS на правки шапки/участников STM SKU, каскад дат
    правок этапов, синк кэша STM с общим редактором задач). См. запись в
    PROGRESS.md за 2026-07-15.

### ⬜ Всё ещё открыто
2. **Системный прогон** — формального сплошного QA-прохода не было;
   сейчас работаем реактивно по репортам пользователей (см. STM-баги
   выше) — работает, но не исчерпывающе.
5. **SMTP** — `SMTP_PASS=ЗАМЕНИТЬ_ПОЗЖЕ`, реального пароля
   noreply@justtodoit.ru (smtp.yandex.ru) до сих пор нет. Вместо починки
   SMTP сделан обход: регистрация новых пользователей через Telegram-бота
   (`/register`, см. supabase/functions/telegram-webhook). Reset-password
   письма всё ещё не уходят — сброс пароля пользователю сейчас делается
   вручную через SQL/Telegram, не самообслуживанием.
9. **email_queue функции указывают на облако** — проверено 2026-07-15:
   `email_queue_dispatch()` до сих пор дёргает
   `https://nvfioycpwyzwukvokwql.supabase.co/functions/v1/process-email-queue`
   (чужой Lovable-инстанс). На VPS это либо тихо фейлится, либо шлёт в
   никуда. Нужно либо переписать на локальный edge-function + реальный
   SMTP, либо отключить cron `process-email-queue`, пока SMTP не готов.
10. **www.justtodoit.ru** — до сих пор на `185.158.133.1` (не VPS),
    `canChange:false` в SpaceWeb DNS — нужна заявка в поддержку SpaceWeb.
11. **Самохостинг шрифтов** — `fonts.googleapis.com` блокируется из РФ
    (косметика, откат на системный шрифт уже работает как fallback).
12. **Google-зависимости (FCM Push)** — не проверено на смоук-тесте,
    вероятно не работает из иностранного VPS; Telegram — основной канал,
    так что не блокер.

## Инцидент: облако Lovable продолжало слать отчёты (2026-07-25, устранён)

Пользователь заметил, что еженедельный отчёт по проекту «Retail Week 2026»
продолжает приходить в Telegram. Проверка облачной БД `nvfioycpwyzwukvokwql`
через Lovable MCP показала: **в облаке остались 5 АКТИВНЫХ cron-задач**, все
бьющие в облачные же edge-функции тем же ботом:

| jobname | schedule |
|---|---|
| auto-baseline-lock-hourly | `0 * * * *` (менял данные!) |
| protocol-buffer-flush | `* * * * *` |
| send-weekly-review-friday | `8 5 * * 5` |
| send-weekly-group-report-friday | `8 5 * * 5` |
| send-weekly-ai-review-friday | `8 5 * * 5` |

Данные в облаке заморожены на ~05.07 → команда получала отчёты, построенные
на устаревших данных, не зная об этом. Плюс `auto-baseline-lock-hourly`
ежечасно менял облачную копию, то есть она уже не была чистым слепком.

**Действие:** все 5 задач сняты (`cron.unschedule`), проверено — `cron.job`
пуст. Облако осталось как архив данных, но больше ничего не шлёт и не меняет.
Определения для отката сохранены ЛОКАЛЬНО (не в git — содержат anon-key
облака), см. scratchpad `cloud-cron-rollback.md`.

**Следствие для бэклога #9:** `email_queue_dispatch()` на VPS всё ещё указывает
на облачный URL — но теперь облако молчит, значит эти вызовы уходят в никуда
гарантированно. Пункт остаётся открытым.

**Осталось проверить:** что VPS-cron реально шлёт еженедельные отчёты
(`SELECT jobname, schedule, active FROM cron.job;` на VPS + `weekly_send_log`
за последнюю пятницу). Если нет — отчётов не будет вообще.

---

## Правило разработки на будущее

Любые изменения схемы БД — **только через миграции в git**, никогда через
ручной SQL-редактор Lovable. Иначе объект не попадёт в git и не доедет до VPS
(так и появилась проблема с таблицей `clients`). Данные живут только на VPS,
код течёт через git+авто-деплой.
